[package]
name = "unai"
version = "0.1.0"
edition = "2021"
description = "Remove LLM-isms from text and code"
license = "MIT"
repository = "https://github.com/username/unai"

[[bin]]
name = "unai"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
